# Patient Portal - PDF Upload App

## Overview
A simple full-stack application to upload, list, download, and delete PDF medical documents.

## Backend
- Node.js + Express + SQLite
- Stores files in `backend/uploads/`
- Metadata stored in `backend/db.sqlite`

### Run Backend
```bash
cd backend
npm install
node server.js
